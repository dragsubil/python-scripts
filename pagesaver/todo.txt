* iterate through the 'namelist' index to open the files one by one
*use corresponding dict values to replace links at previous chapter and nextchapter

NOTE: we are able to iterate through the namedict itself. we might not need the namelist